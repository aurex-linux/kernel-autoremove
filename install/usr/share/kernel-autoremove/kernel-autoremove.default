# kernel-autoremove defaults file
# change values in this file or override by creating /etc/kernel-autoremove.conf

#set the number of latest kernels to preserve (invalid values or numbers less than 1 are ignored)
PRESERVED_KERNELS_NUM=2

#blank delimited optional whitelist of kernel versions to preserve (fixed strings)
KERNEL_VERSION_WHITELIST=""
